# Deploy to Render (render.com) — free tier
# Push this repo to GitHub, then connect it on render.com
services:
  - type: web
    name: cinematch-movie-recommender
    env: python
    buildCommand: |
      pip install -r requirements.txt &&
      python -c "import nltk; nltk.download('wordnet'); nltk.download('stopwords')"
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
